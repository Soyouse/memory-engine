{
  "name": "memory-engine",
  "version": "0.1.0",
  "description": "Mémoire long terme sémantique 100% locale pour Claude Code (embeddings llama.cpp GPU/CPU, compaction-aware). Tests node:test, mutation Stryker sur les noyaux purs.",
  "license": "Apache-2.0",
  "repository": { "type": "git", "url": "https://github.com/Soyouse/memory-engine.git" },
  "keywords": ["claude-code", "plugin", "memory", "semantic", "embeddings", "llama.cpp", "local", "rag"],
  "engines": { "node": ">=18" },
  "scripts": {
    "test": "node --test",
    "test:watch": "node --test --watch",
    "mutation": "stryker run",
    "test:mutation": "stryker run"
  },
  "devDependencies": {
    "@stryker-mutator/core": "^8.7.1",
    "@stryker-mutator/tap-runner": "^8.7.1"
  }
}
